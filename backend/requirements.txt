# ============================================================
# GoodCang Cost Intelligence 后端依赖
# 说明：按功能分组，便于审阅与裁剪
# ============================================================

# ---- Web 框架 ----
fastapi>=0.110          # REST API 框架
uvicorn[standard]>=0.29 # ASGI 服务器

# ---- 数据库 ----
SQLAlchemy>=2.0           # ORM
psycopg[binary]>=3.1      # PostgreSQL 驱动（psycopg3；Windows 环境无 psycopg2-binary 时使用）
alembic>=1.13           # 数据库迁移

# ---- 配置 / 数据校验 ----
pydantic>=2.6           # 数据模型
pydantic-settings>=2.2  # .env 配置管理

# ---- HTTP 客户端（调用 GoodCang API）----
httpx>=0.27             # 异步 HTTP，带超时/重试
tenacity>=8.2           # 重试策略

# ---- 任务调度（数据同步）----
APScheduler>=3.10       # 定时同步任务

# ---- 测试 ----
pytest>=8.0
pytest-asyncio>=0.23
